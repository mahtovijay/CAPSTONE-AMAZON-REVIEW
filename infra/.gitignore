# --------------------------------------------------------------------
# Terraform bootstrap ignore list
# Prevent local state files, backups, and sensitive data from being
# committed to Git. This folder manages only the remote backend setup.
# --------------------------------------------------------------------

# Local Terraform state
terraform.tfstate
terraform.tfstate.*

# Crash logs
crash.log

# Terraform directories
.terraform/
.terraform.lock.hcl

# Local variable overrides
*.tfvars
*.tfvars.json

# Sensitive auto-generated files
*.auto.tfvars
*.backup

# Ignore backend file if local only (use backend.tf.template instead)
#backend.tf

# OS/system files
.DS_Store
Thumbs.db